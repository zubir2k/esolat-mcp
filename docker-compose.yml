services:
  esolat-mcp:
    image: esolat-mcp:latest
    container_name: esolat-mcp
    restart: unless-stopped
    ports:
      - "8626:8626"
    environment:
      - PORT=${PORT:-8626}
      - MCP_WEBHOOK_TOKEN=${MCP_WEBHOOK_TOKEN}
      - MCP_TRANSPORT=http
